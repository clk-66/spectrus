{
  "name": "@spectrus/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"pnpm --filter @spectrus/client dev\" \"tauri dev\"",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@spectrus/client": "workspace:*",
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "concurrently": "^9.0.0"
  }
}
